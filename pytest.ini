[pytest]
addopts = --cov=app --cov-report=html --cov-report=xml --cov-report=term-missing --alluredir=allure-results 
markers =
    integration: tests that hit real external services (db/dolibarr/smtp)
